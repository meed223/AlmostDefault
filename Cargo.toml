[package]
name = "almostdefault"
version = "0.2.0"
edition = "2024"
license-file = "LICENSE"
readme = "README.md"

[dependencies]
walkdir = "2"
image = "0"
clap = { version = "4", features = ["derive"] }
tokio = { version = "1", features = ["full"] }